# SPDX-License-Identifier: GPL-3.0-or-later
# Dedicated system user owning the shared Homebrew prefix.
# UID < 1000 keeps it invisible to gnome-initial-setup, gdm, accountsservice.
u linuxbrew - "Homebrew system user" /var/home/linuxbrew /usr/sbin/nologin

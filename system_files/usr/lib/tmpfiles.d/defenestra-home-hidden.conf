# SPDX-License-Identifier: GPL-3.0-or-later
# Hide system-managed home directories from Nautilus and other file managers
# that respect the .hidden convention. linuxbrew owns /var/home/linuxbrew
# (shared brew prefix) but isn't a real interactive user.
f /var/home/.hidden 0644 root root - linuxbrew

# SPDX-License-Identifier: GPL-3.0-or-later
# Expose Nix profile dirs to the systemd user session (GNOME Shell, portals,
# user services). User profile shadows system profile.
NIX_PROFILES=/nix/var/nix/profiles/default ${HOME}/.nix-profile
PATH=${HOME}/.nix-profile/bin:/nix/var/nix/profiles/default/bin:${PATH}
NIXPKGS_ALLOW_UNFREE=1
# Surface host Mesa/Vulkan/NVIDIA userspace to nix-built GPU apps. The dir
# is curated (GPU drivers + direct deps only), safe from shadowing core libs.
LD_LIBRARY_PATH=/run/opengl-driver/lib

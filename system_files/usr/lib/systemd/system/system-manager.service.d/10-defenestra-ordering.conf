# SPDX-License-Identifier: GPL-3.0-or-later
# After nix.mount / nix-daemon: builder resolves store paths and talks to the daemon.
[Unit]
After=nix.mount nix-daemon.service
Requires=nix.mount
Wants=nix-daemon.service
